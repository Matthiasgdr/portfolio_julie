section
    width 100%
    height 100vh
.first-page
    display flex
    align-items center
    justify-content center
    .me
        position relative
        display flex
        flex-direction: column
        align-items center
        h1 
            font-size: 40px
        h2 
            font-size: 20px
        .seework
            display flex
            flex-direction: column
            align-items center
            transform: translateY(20vh)
            p  
            font-size: 20px
        .down_arrow
            width: 40px
            height 40px
        .losange
            position absolute
            top 0
            left 0
            transform: translateY(-45%) translateX(-36%) rotate(45deg);
            transform-origin center;
            width 50vh
            height 50vh
            border 60px solid #1e1e1e